/**
 * API Configuration
 * 
 * เมื่อต้องการเปลี่ยนไปใช้ API จริง:
 * 1. เปลี่ยน USE_MOCK_API เป็น false
 * 2. แก้ไข API_BASE_URL ให้ชี้ไปที่ API server ของคุณ
 */

export const USE_MOCK_API = true // เปลี่ยนเป็น false เมื่อพร้อมใช้ API จริง
export const API_BASE_URL = '/api' // เปลี่ยนเป็น URL ของ API server ของคุณ

